#include<stdio.h>
int main(){
  int r = 7;
  int height = 6;
  printf("The area of the circle with radius %d is %f",r,3.14*r*r);
  printf("The volume of  cylinder with radius %d height %d is %f",r,height,3.14*r*r*height);
  return 0;
}